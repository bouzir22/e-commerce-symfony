{% extends 'base.html.twig' %}

{% block title %}Hello CartController!
{% endblock %}

{% block body %}
    <div class="bg-light p-3">
        <h1>Votre panier</h1>
    </div>

{% if items | length > 0 %}

<table class="table">
<thead><tr>
    <th>Produit</th>
    <th>Prix</th>
    <th>Quantité</th>
    <th>total</th>
  </tr>
  </thead>
  <tbody>
  
 {% for item in items %} 
  
   <tr>
    <td>{{item.product.title}}</td>
    <td>{{item.product.price}}</td>
    <td>{{item.quantity}}</td>  
    <td>{{item.product.price*item.quantity}}</td>
    <td><a href ="{{path('cart_remove',{id:item.product.id})}}" class="btn btn-danger btn-sm"> <i class="fas fa-trash"></i></a>

    </td>
  </tr>
  {% endfor %}
  </tbody>

  <tfoot>
  <td colspan="3" class="test-right" > Total : </td>
  <td>{{total}}</td>
  
  </tfoot>
 
</table>




 <td><a href ="{{path('user_buy')}}"
  class="btn btn-warning btn-lg"> confirm <i class="fas fa-credit-card"></i></a>





{% else %}
    <p>Surprise, c'est vide !</p>

    {% endif %}
{% endblock %}
